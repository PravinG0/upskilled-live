{
  "buildCommand": "npx vite build --config vite.static.config.ts",
  "outputDirectory": "dist/static",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
